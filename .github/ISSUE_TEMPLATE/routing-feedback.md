---
name: 路由反馈（误触发 / 漏触发）
about: 某个 skill 在不该触发时触发了，或该触发时没触发
labels: routing
---

**类型**（留一个）：误触发（不该触发却触发了）/ 漏触发（该触发却没触发）

**涉及的 skill**：

**任务原话**（当时发给助手的话，尽量原样贴）：

**期望行为**：

**实际行为**：

**模型**：

**harness**：Claude Code / Codex / 其它

**是否显式调用过 skill**：是 / 否
